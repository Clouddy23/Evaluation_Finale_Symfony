{% extends 'base.html.twig' %}

{% block title %}PaymentMethod{% endblock %}

{% block body %}
    <h1>PaymentMethod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment_method.id }}</td>
            </tr>
            <tr>
                <th>PaymentMethod</th>
                <td>{{ payment_method.PaymentMethod }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ payment_method.Category }}</td>
            </tr>
            <tr>
                <th>TransactionUser</th>
                <td>{{ payment_method.TransactionUser ? payment_method.TransactionUser|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_payment_method_index') }}">back to list</a>

    <a href="{{ path('app_payment_method_edit', {'id': payment_method.id}) }}">edit</a>

    {{ include('payment_method/_delete_form.html.twig') }}
{% endblock %}
